{
  "reader": {
    "type": "basic",
    "format": "csv",
    "options": {
      "header": "false",
      "delimiter": ","
    },
    "schema": "sepal_length DOUBLE, sepal_width DOUBLE, petal_length DOUBLE, petal_width DOUBLE, class STRING"
  },
  "result_reader": {
    "type": "basic",
    "format": "json"
  },
  "component": {
    "type": "Pipeline",
    "stages": [
      {
        "type": "StringIndexerModel",
        "@path": "target/tmp/xgboost/data/stages/0_strIdx_29852d94318f"
      },
      {
        "type": "VectorAssembler",
        "inputCols": ["sepal_length", "sepal_width", "petal_length", "petal_width"],
        "outputCol": "features"
      }
    ]
  },

  "test": "toy_dataset/iris/iris.data",
  "writer": {
    "type": "sql",
    "sqls": [
      "drop table if exists ${table}",
      "create table if not exists ${table} as select * from __THIS__"
    ]
  }
}